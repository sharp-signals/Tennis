name: RapidAPI Odds Audit

on:
  workflow_dispatch:
    inputs:
      player_a:
        description: Primeiro jogador, como aparece no fixture
        required: true
        type: string
      player_b:
        description: Segundo jogador, como aparece no fixture
        required: true
        type: string
      event_date:
        description: Data do evento (YYYY-MM-DD)
        required: true
        type: string
      tour:
        description: Tour
        required: true
        type: choice
        options: [atp, wta]
      player_a_id:
        description: ID estável do primeiro jogador
        required: true
        type: string
      player_b_id:
        description: ID estável do segundo jogador
        required: true
        type: string
      audit_the_odds:
        description: Comparação controlada The Odds API (OFF por defeito)
        required: false
        default: false
        type: boolean

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.11"
          cache: pip
          cache-dependency-path: requirements.lock
      - name: Instalar dependências
        run: pip install -r requirements.lock
      - name: Auditar rotas de odds RapidAPI
        env:
          RAPIDAPI_KEY: ${{ secrets.RAPIDAPI_KEY }}
          RAPIDAPI_MAX_CALLS_PER_RUN: "20"
          RAPIDAPI_MAX_CALLS_PER_DAY: "4500"
          ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
          THE_ODDS_API_ENABLED: ${{ inputs.audit_the_odds && '1' || '0' }}
          LLM_MODE: disabled
          LLM_POLICY: never
          ALLOW_PAID_LLM: "0"
        run: >-
          python -m scripts.audit_rapidapi_odds
          --player-a "${{ inputs.player_a }}"
          --player-b "${{ inputs.player_b }}"
          --event-date "${{ inputs.event_date }}"
          --tour "${{ inputs.tour }}"
          --player-a-id "${{ inputs.player_a_id }}"
          --player-b-id "${{ inputs.player_b_id }}"
          ${{ inputs.audit_the_odds && '--audit-the-odds' || '' }}
      - name: Guardar resultado da auditoria
        uses: actions/upload-artifact@v7
        with:
          name: rapidapi-odds-audit
          path: artifacts/rapidapi_odds_audit/result.json
          if-no-files-found: error
